<%= form_for(@admin_dispatcher) do |f| %>
  <% if @admin_dispatcher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_dispatcher.errors.count, "error") %> prohibited this admin_dispatcher from being saved:</h2>

      <ul>
      <% @admin_dispatcher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
